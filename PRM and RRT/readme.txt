Install the additional libraries required to run the program.

Steps to install libraries:
1) Go to command prompt and type the commands of step 2 and 3
2) pip install seaborn
3) pip install pandas